datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Links {
  token Int     @unique()
  value String
  used  Boolean @default(false)
}
